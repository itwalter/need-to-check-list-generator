{
  "name": "@cofacts/need-to-check-list-generator",
  "version": "1.0.0",
  "description": "Generate the list of need to fact-checking articles, which is used to dispatch tasks in meetup",
  "engines": {
    "node": "^7.7.3"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lucien Lee",
  "repository": {
    "type": "git",
    "url": "https://github.com/cofacts/need-to-check-list-generator"
  },
  "license": "MIT",
  "dependencies": {
    "command-line-args": "^4.0.7",
    "mkdirp": "^0.5.1",
    "request": "^2.81.0"
  }
}
